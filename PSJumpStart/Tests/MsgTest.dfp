#
#Global default parameters for function "msg"
#
# Set verbose for msg function
msg:Verbose=$true
# Set default type to ERROR
#msg:Type="ERROR"

# Use Eventlog for messages
msg:useEventLog=$false
msg:EventLogName="PSJumpstart"

# Use File-logging (both may be active at once)
msg:useFileLog=$true
#msg:logFile=$(Split-Path -Parent $MyInvocation.PSCommandPath) + "\Logs\" + $(Split-Path -Leaf $MyInvocation.PSCommandPath) + "." + (Get-Date -Format 'yyyy-MM') + ".Log"


